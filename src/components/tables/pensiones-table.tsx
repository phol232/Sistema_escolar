import { DataTable } from "@/components/tables/data-table";

export function PensionesTable() {
  return <DataTable title="Tabla de pensiones" description="Pendiente de conectar pagos, estados y filtros por periodo." />;
}
