import { DataTable } from "@/components/tables/data-table";

export function NotasTable() {
  return <DataTable title="Tabla de notas" description="Pendiente de integrar promedios y edición por evaluación." />;
}
