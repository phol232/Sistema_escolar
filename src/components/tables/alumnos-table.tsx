import { DataTable } from "@/components/tables/data-table";

export function AlumnosTable() {
  return <DataTable title="Tabla de alumnos" description="Pendiente de conectar TanStack Table con paginación y filtros server-side." />;
}
