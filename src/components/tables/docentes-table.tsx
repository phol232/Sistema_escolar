import { DataTable } from "@/components/tables/data-table";

export function DocentesTable() {
  return <DataTable title="Tabla de docentes" description="Pendiente de agregar columnas, filtros y acciones por rol." />;
}
