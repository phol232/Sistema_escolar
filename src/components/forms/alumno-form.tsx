import { EmptyState } from "@/components/shared/empty-state";

export function AlumnoForm() {
  return (
    <EmptyState
      title="Formulario de alumno"
      description="Base preparada para React Hook Form + Zod con datos de persona y alumno."
    />
  );
}
