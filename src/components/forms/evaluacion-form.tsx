import { EmptyState } from "@/components/shared/empty-state";

export function EvaluacionForm() {
  return (
    <EmptyState title="Formulario de evaluación" description="Pendiente de registrar evaluaciones por curso, sección y bimestre." />
  );
}
